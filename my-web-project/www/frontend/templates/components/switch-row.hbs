<tr key="{{id}}">
  <td><a href="#view-switch?id={{id}}">{{title}}</a></td>
  <td>{{AliasUser}} ({{user.currentUser}})</td>
  <td>

    {{#if_eq user.currentUser AliasUser}}

      <button class="remove-button" item="{{id}}">{{i18n 'remove'}}</button>
      <button class="edit-button" item="{{id}}">{{i18n 'edit'}}</button>
    {{/if_eq}}
  </td>
</tr>